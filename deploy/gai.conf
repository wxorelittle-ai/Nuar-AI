# Предпочитать IPv4 при разрешении имён (getaddrinfo).
# Нужно, когда у сервера нет рабочего IPv6-маршрута, а домены (напр.
# api.telegram.org) отдают и AAAA: без этого попытка идти по IPv6 даёт
# «Network is unreachable». Строка ниже поднимает приоритет IPv4.
precedence ::ffff:0:0/96  100
