# ── РестоПульс · Competitor Scraper Agent — зависимости ──────────────
# HTTP-клиент (синхронный, с ретраями)
httpx==0.27.2
# Парсинг HTML-карточек 2ГИС / Яндекс (fallback, когда нет API-ключа)
beautifulsoup4==4.12.3
# Чтение config/competitors.yaml
PyYAML==6.0.2
# Загрузка .env
python-dotenv==1.0.1
# Планировщик еженедельного запуска
APScheduler==3.10.4
# Работа с PostgreSQL (опционально; без DATABASE_URL не требуется)
psycopg[binary]==3.2.3

# ── Веб-интерфейс + production-сервер ────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
# WSGI/ASGI-сервер для прода (Linux/Docker; на Windows локально — uvicorn)
gunicorn==23.0.0

# ── dev / tests ───────────────────────────────────────────────────────
pytest==8.3.3
